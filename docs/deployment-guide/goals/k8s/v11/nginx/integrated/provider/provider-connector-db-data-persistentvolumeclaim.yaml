apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: provider-connector-db-data
  name: provider-connector-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
