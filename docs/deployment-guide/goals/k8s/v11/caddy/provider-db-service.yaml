apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: provider-connector-db
  name: provider-connector-db
spec:
  ports:
    - name: "5432"
      port: 5432
      targetPort: 5432
  selector:
    io.kompose.service: provider-connector-db
  type: ClusterIP
