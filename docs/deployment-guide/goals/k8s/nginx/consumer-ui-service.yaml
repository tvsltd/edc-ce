apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: consumer-ui
  name: consumer-ui
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 8080
  selector:
    io.kompose.service: consumer-ui
