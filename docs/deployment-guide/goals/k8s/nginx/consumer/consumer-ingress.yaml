apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: consumer-ingress
  namespace: master
spec:
  ingressClassName: nginx
  rules:
    - host: consumer.master.svc.cluster.local
      http:
        paths:
          - path: /api/management
            pathType: Prefix
            backend:
              service:
                name: consumer-cp
                port:
                  number: 11002
          - path: /api/dataplane
            pathType: Prefix
            backend:
              service:
                name: consumer-dp
                port:
                  number: 11003
          - path: /
            pathType: Prefix
            backend:
              service:
                name: consumer-ui
                port:
                  number: 80
