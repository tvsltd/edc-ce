apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: consumer
  name: consumer
spec:
  ports:
    - name: "22000"
      port: 22000
      targetPort: 80
  selector:
    io.kompose.service: consumer
