apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: provider
  name: provider
spec:
  ports:
    - name: "11000"
      port: 11000
      targetPort: 80
  selector:
    io.kompose.service: provider
